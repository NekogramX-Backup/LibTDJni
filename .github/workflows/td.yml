name: TDLib CI

on:
  push:
    branches: [ master ]

jobs:
  TDLib:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build TDLib
      run: docker run -t .:/td debian:buster bash /td/build.sh